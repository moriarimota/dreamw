# 《那边的小日子》开发协作规则

## 从这里开始，不要重读全部聊天

先读 `docs/00-INDEX.md`、`docs/STATUS.md`、`docs/architecture/01-system.md`；只按索引读取当前改动涉及的设计章节。用户是制作人，用户在当前会话的明确决定优先于旧提案。本文件不要求每次通读知识库。

## 不能丢失的方向

- 她独自生活与玩家加入，是同一段持续生活的两种状态。无每日义务、离线惩罚、错过重要剧情的倒计时。
- 生活碎片统称“梦种”。不得恢复旧术语“回响”。《那边的小日子》是暂用名；小罗是暂用角色名。
- 世界状态、时间、规则与记忆是真实来源；AI理解素材、提出有限建议、表达事实，不能直接写世界状态、奖励、记忆或规则代码。
- 重要行为必须有理由；硬条件先检查，再进行带少量随机性的倾向选择。随机状态入档，同一段离线时间不能重复结算。
- 保存梦种原文、经历记忆与长期知识的区分，保留来源ID和因果关系。普通移动不刷手记。
- 保留用户原创角色、当前暖色手绘像素风。圣诞是冬服，星月和红斗篷为常驻特征。不要未经请求改画风或重做全项目。

## 工作与文件

- 主项目在 `D:/WitchLife`。游戏代码在 `game/`，桌面源码在 `desktop/`，真实存档在 `user-data/`，不可放回 C 盘开发目录。
- `archive/` 是历史档案，不作为当前需求真相；当前版本状态只更新 `docs/STATUS.md`。
- 新想法先追加 `docs/design/02-ideas-inbox.md`，注明来源、状态、影响模块；采纳后更新对应设计/架构文档与 `docs/CHANGELOG.md`。
- 改存档结构必须包含迁移/校验和旧档回归。不要以重置玩家存档来修复错误。
- 禁止提交 user-data、浏览器资料、真实梦种、导出存档、令牌、构建测试残留。打包使用 `tools/package.ps1` 的明确清单。
- 图像生成遵循 imagegen 技能；精灵贴图不视为已合格动画，检查透明通道、格子、帧对齐与实际比例。
- 基础日常不能依赖每秒模型调用。没有接上在线AI时必须明说本地规则模式。

## 检查和交付

- 逻辑：`tests/life-tests.js`；存档：`tests/storage-tests.js`；棋局：`tests/gomoku-tests.html`；整页：`tests/ui-smoke.html`；寻路：`game/tests.html`；桌面：`desktop/smoke.ps1`（具体以 desktop/README.md 为准）。
- 用户界面改动需要实际打开浏览器查看并操作；工具失败时明确区分实现与验证，不能写“全部已测”。
- 发布前检验包不含私人存档与原始素材；GitHub Pages 只发布静态游戏。账号云同步、自动AI调用、原生iOS并非天然存在。
- 说明本次改变、测试、限制；保持索引、状态、决策记录与代码一致。
