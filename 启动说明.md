# 那边的小日子 · 电脑启动说明

双击本文件旁边的 **启动那边的小日子.exe**。请保留 `game` 文件夹和启动程序的相对位置。

- 使用已安装的 Edge 或 Chrome 打开独立游戏窗口；首次打开可能需要几秒钟。
- 启动程序没有控制台窗口，也不会安装系统服务或设置开机启动。
- 关闭游戏窗口后，本机服务仍留在右下角托盘。双击托盘图标可以重新打开；右键选择“退出游戏服务”可完全退出。
- 游戏运行地址为 `http://127.0.0.1:18765/`，只接受这台电脑的连接，不会公开到互联网。
- 再次双击会复用这份游戏已经运行的服务，不会关闭其他程序。

## 文件与存档

游戏资源、启动程序和游戏专用浏览器资料都保存在当前游戏目录。当前目录为 `D:\WitchLife`。

- `game`：游戏网页及素材。
- `user-data/state.json`：桌面版游戏存档。
- `user-data/state.previous.json`：上一次保存的备份。
- `user-data/browser`：游戏专用 Edge/Chrome 用户资料。
- `user-data/browser-cache`：游戏专用浏览器缓存。
- `user-data/launcher.log`：启动异常记录（出现异常时才生成）。

游戏目录可以整体复制到其他位置。移动前先退出托盘服务，并在游戏内导出一份存档备份。浏览器可能保护部分与机器或账户有关的数据；跨设备建议使用游戏内的导入/导出功能。

若电脑没有 Edge 或 Chrome，启动程序会提示并使用默认浏览器；默认浏览器自身的缓存路径由其设置决定，可能仍在 C 盘。Windows 和浏览器本体也可能正常写入系统临时数据。启动程序不移动或修改这些系统文件。

这个 `.exe` 适用于 Windows。iPhone 使用网页版本；桌面与手机的存档目前需要通过导出/导入转移，尚未接入云同步。

## 重新编译

源代码为 `desktop/Launcher.cs`。在 PowerShell 中运行 `desktop/build.ps1`，使用 Windows 自带 .NET Framework C# 编译器生成启动程序，不依赖 Python 或 Node.js。

测试时可使用 `启动那边的小日子.exe --headless`，只启动本机服务，不打开浏览器或托盘。测试结束时需停止该测试进程。
